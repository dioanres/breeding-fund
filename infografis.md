*** Menu Infografis

- Create New Menu Infografis on sidebar admin cms
- create table and CRUD function form for this feature, column is name, file, type (image/pdf), is_active,timestamps
- Create New Menu on Portal navbar for public is "Infografis"
- show Data infografis from DB, if data file image then show image, if pdf then show pdf, and also possible for download
- add new column published_at and can editable by user
- in portal view "dibuat pada" change from this data
- set this data optional if empty then get from created_at, else get from published_at
- generate 1 route to get detail image by id in page portal